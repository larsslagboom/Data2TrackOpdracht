module.exports = {
    // ... other configurations
    rules: {
        // Disable specific rules or entire categories
        'no-console': 'off', // Disable console.log() errors
        'no-unused-vars': 'off', // Disable unused variable errors
        // Add more rules here as needed
    },
};
